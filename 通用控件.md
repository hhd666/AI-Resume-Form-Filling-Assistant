# 通用控件适配（所有网站，框架无关）支持说明

本文档记录 content.js 对**任意网站自定义控件**的通用适配机制：不依赖任何特定框架（React/Vue/自研）的 class 前缀，通过「遍历 input 自身及其兄弟/父元素找 `click` 激活容器 + `invalid` 事件确认输入」自动完成填写。适用于 antd / Element / Moka / atsx 之外的各种网申、招聘、报名表单。

---

## 1. 核心思想

大多数框架自定义控件的通病是：**直接给 `<input>` 写 `value` 网页不认可**（React/Vue 受控组件不更新 state、提交时仍为旧值）。但框架控件通常有两个共同特征：

1. **`click()` 激活**：input 自身或其**兄弟/父元素**（label、选择器容器、包装层）绑定了 `click` 处理器——点击后才进入"可接受输入"的状态（或弹出选择面板）。
2. **`invalid()` 确认**：input 上监听 `invalid` 事件（配合 `checkValidity()` 约束校验）来把脚本写入的值确认为"真实输入"并同步内部 state。

通用适配即围绕这两点自动工作：**先点击激活容器 → 看是否弹出选择面板（有则选值，无则直接写值）→ 触发 invalid 确认**。年月日等选择器还自动分辨年/月/日并提取目标值，无需逐个控件适配。

---

## 2. 点击激活容器检测

### 2.1 `isClickActivator(candidate, inputEl)`（content.js:2664）

判定一个元素是否是 input 的"点击激活容器"：

| 目标 | 判定条件 |
|---|---|
| input **自身** | `readonly`、显式绑定 `onclick`（属性或 `getAttribute("onclick")`）、class 含明确自定义日期输入特征（`datepicker`/`flatpickr`/`picker-input`/`calendar-input`/`date-input`）；原生日期类 input（`type=date/month/week/time` 等）直接写值即可，不算激活器 |
| 兄弟/父元素 | 显式 `onclick` 绑定；`<label>` 包裹 input 或 `for` 关联（`for` 值等于 input 的 `id`）；`role="combobox"/"listbox"`；class 含通用选择器特征（`select`/`picker`/`dropdown`/`combobox`/`date-picker`/`calendar`/`input-container`/`input-wrapper`/`input-wrap`/`inputbox`）；选择触发器按钮（class 含 select/picker/date/calendar/dropdown/arrow/icon/toggle 的 button） |
| **排除** | class 含 `atsx-` 的元素（atsx 是专用适配，见 §6）；`input`/`textarea` 自身在"兄弟/父"分支直接排除 |

### 2.2 `findClickActivator(el, maxDepth = 4)`（content.js:2708）

遍历顺序：**input 自身 → 父链（由近及远，最多 4 层）→ 每层父的 label 兄弟**。兄弟只考虑 `<label>`（`for` 关联），其余兄弟（搜索框、前缀/后缀图标、箭头等）多为装饰、不承载 click，避免误点。

```js
// 示例：antd Select
<input class="ant-select-selection-search-input">      ← 自身无特征
  ↑ 父
<div class="ant-select-selector">                      ← class 含 "select" → 命中激活器
```

---

## 3. `invalid` 确认

### `dispatchInvalidEvent(el)`（content.js:2420）

```js
el.dispatchEvent(new Event("invalid", { bubbles: true, cancelable: true }));
el.checkValidity?.();   // 原生约束校验，触发真实 invalid 事件流
```

页面里无法枚举 `addEventListener("invalid")` 监听，因此对走通用路径的控件一律**写值后触发 invalid + checkValidity**——有监听的控件据此确认输入，无监听的零副作用。

---

## 4. 填充流程

### 4.1 `fillOne` 分流顺序（content.js:1549 起）

```
1. Moka（mokahr）sd-Select 下拉   → fillMokaSelect（专用，§7.2）
2. atsx 日期控件                   → fillAtsxPeriodMonth（专用，见 atsx-date-picker-support.md）
3. 通用激活控件（其他网站）        → fillGeneric（本机制核心）
4. 普通写值                       → setValueWithEvents（label 激活 + invalid + 事件序列）
```

第 3 步条件：`!isAtsxControl(runtime.el) && findClickActivator(runtime.el)`——atsx 被显式排除。

### 4.2 `fillGeneric(runtime, desired)`（content.js:2854）

```
findClickActivator(el) ── 无激活容器 → 返回 false（走第 4 步普通写值）
        │
        ▼
记录点击前的可见面板集合（collectVisiblePanels）
clickLikeUser(activator) 点击激活容器
sleep(160ms)
findNewPanel(before, activator, el) 对比面板集合差异
        │
        ├─ 弹出了选择面板 →
        │    getMokaPartValue(runtime, desired) 自动分辨年/月/日提取目标值
        │    clickGenericOption(panel, target) 面板内选值
        │    → 收起（Escape）→ verifyWritten 校验
        │
        └─ 无面板（或面板无匹配项）→
             setValueRealistic(el, desired) 写值（原生 setter + execCommand/事件序列）
             dispatchInvalidEvent(el)       触发 invalid 确认
             verifyWritten 校验
```

失败自动回退：面板打不开/选项匹配不到 → 返回 false → 由 `fillOne` 落到第 4 步普通写值，不会误填也不会阻塞。

### 4.3 面板检测

| 函数 | 行号 | 说明 |
|---|---|---|
| `isPanelLike(node)` | 2733 | `role` 为 listbox/menu/option/combobox/popup/dialog；class 词边界含 dropdown/menu/option/listbox/popup/picker/panel/calendar；或 class 含 select-dropdown 等特征 |
| `collectVisiblePanels()` | 2744 | 收集当前可见面板，**排除所有 `atsx-` 元素**（atsx 面板注入页面末尾，不能进通用逻辑） |
| `findNewPanel(before, activator, el)` | 2755 | 对比点击前后集合取"新增面板"，按与激活器的距离取最近；排除包含/被包含于激活器的节点 |

### 4.4 面板选值

| 函数 | 行号 | 说明 |
|---|---|---|
| `pickPanelOption(panel, desired)` | 2777 | 遍历 `[role=option]`/`li`/`button`/class 含 option/item/cell 的节点；精确文本匹配优先、数字前导零归一化兜底（`"06"`≡`"6"`）；取文本最短的叶子节点 |
| `clickGenericOption(panel, desired)` | 2810 | `scrollIntoView` + `clickLikeUser` 点击目标项 |
| `dismissGenericPanel()` | 2824 | Escape keydown/keyup 收起面板 |
| `verifyWritten(el, desired)` | 2842 | 校验 `el.value`：精确相等或数字前导零归一化相等 |

### 4.5 日历面板选值（年月切换 + 日期表格）

对 **antd 等"日历类"面板**（点击 readonly input 弹出的年月表格），通用流程先识别日历面板再走专用选值：

| 函数 | 行号 | 说明 |
|---|---|---|
| `isCalendarPanel(panel)` | 2907 | class 含 `calendar`/`date-panel`/`datepicker`/`picker-panel`，或面板内存在日期单元格（`td[title*="年"][title*="月"]`）/年月切换按钮 |
| `readCalendarHeader(panel)` | 2920 | 读面板当前年月：优先 `year-select`/`month-select` 文本（`"2024年"`/`"9月"`），兜底从日期格 `title`（`"2024年9月1日"`）解析 |
| `switchCalendarToYearMonth(panel, y, m)` | 2942 | 用 `prev/next-month-btn` 把面板切到目标年月（最多 24 步，前后各 12 个月） |
| `findCalendarDayCell(panel, y, m, d)` | 2962 | 找目标日：`td[title="2024年9月1日"]` 精确匹配优先；兜底遍历 `td` 按 `title` 解析或同月内文本匹配（排除上/下月占位格）；返回 `div.ant-calendar-date` 或 `td`（二者带 click） |
| `fillCalendarLike(panel, desired)` | 2990 | 全流程：解析目标（`YYYY`/`YYYY-MM`/`YYYY-MM-DD`）→ 切到目标年月 → 点目标日（仅年月时切好月即可） |

`fillGeneric` 面板分支流程（content.js:2868 起）：
```
弹出面板 → isCalendarPanel(panel)?
  ├─ 是 → fillCalendarLike（切年月 → 点日期）→ 校验（含前缀匹配，支持 YYYY-MM 目标）
  └─ 否 → clickGenericOption（文本匹配选项）
```

示例（antd 3.x 日历）：readonly input `value="2024-09-01"`，点击弹出 `ant-calendar-date-panel`，目标 `2025-03-15` → 点 `next-month-btn` 6 次切到 2025-03 → 点 `td[title="2025年3月15日"]` 内的 `.ant-calendar-date` → 面板关闭、input 更新。

### 4.6 年月日自动分辨

### `getMokaPartValue(runtime, desired)`（content.js:2518）

按 input 的 placeholder/label 提示词（年/月/日/year/month/day）从映射值中提取对应部分作为面板匹配目标：

```
"2020-06-15" → 年控件取 "2020"、月控件取 "6"、日控件取 "15"
"2020年6月"  → 年取 "2020"、月取 "6"
"06"（单段）  → 原样返回，交给 mokaOptionMatches 做前导零归一化
```

无年月日提示 → 原样返回。此函数已被通用流程复用（`fillGeneric` 内调用），Moka 专用路径也用它。

---

## 5. 事件序列与写值

`setValueRealistic(el, value)`（content.js:2903）：`setNativeValue`（`HTMLInputElement.prototype.value` setter + `setAttribute`）→ `focus` + `select` + `execCommand("insertText")` 模拟真实键盘输入 → 失败时回退 `setNativeValue` + `dispatchInputEvents`（compositionstart/input(InputEvent)/compositionend/input/change 完整事件链）。

`setValueWithEvents(el, value, runtime)`（content.js:1612）：统一写值入口——`scrollIntoView` → Moka label 激活（非 select-like 的 sd-Input 先点击 label）→ 写值 → invalid 确认 → `blur` → 校验 `matchesWrittenValue`。

---

## 6. 与 atsx 专用适配的隔离

`isAtsxControl(el)`（content.js:2654）：class 含 `atsx-` 前缀 / `atsx-date-picker-period-hidden-input` / `closest` 命中 `[class*="atsx-date-picker"]`。

通用逻辑对 atsx 全面隔离：
- `fillOne` 第 3 步条件 `!isAtsxControl(...)`——atsx 字段不进 `fillGeneric`；
- `collectVisiblePanels` 排除 atsx 面板——通用面板检测看不到 atsx 注入到页面末尾的日期面板；
- `isClickActivator` 排除 atsx 候选——任何字段的激活器检测都不会把 atsx 容器当点击目标。

atsx 完全由专用逻辑（`fillAtsxPeriodMonth` 等）接管，互不干扰。

---

## 7. Moka（mokahr）专用适配（通用机制的补充）

Moka 是通用机制的"先行案例"（此前逐控件适配后泛化），其专用函数保留在 content.js，逻辑与通用机制一致但更贴合 Moka 结构：

| 函数 | 行号 | 说明 |
|---|---|---|
| `findMokaSdInputLabel(el)` | 2409 | 定位 `label[class*="sd-Input-container-"]`（普通文本输入框的激活容器） |
| `isMokaSdInput(el)` | 2414 | 判定普通文本型 Moka input |
| `findMokaSelectLabel(el)` | 2440 | 定位 `label[class*="sd-Select-container-"]`（下拉选择型） |
| `isMokaSdSelect(el)` | 2445 | 判定明确下拉型 |
| `isMokaSelectLike(el)` | 2457 | **通用判定"点击弹出选择框"**：label 是 sd-Select-container / label 内出现 sd-Select-* 元素 / input 自身带 `sd-Input-has-addon-` |
| `findMokaDropdownPanel(labelEl)` | 2466 | 定位展开的 `.sd-Dropdown-dropdown-*` 面板（优先 label 所在容器内，否则全局最近） |
| `dismissMokaDropdown()` | 2496 | Escape 收起 |
| `mokaOptionMatches(a, b)` | 2550 | 选项匹配：精确相等 / 忽略"年月"后缀 / 数字前导零归一化 |
| `fillMokaSelect(runtime, desired)` | 2566 | 下拉选择全流程：点击 label 展开 → 面板内找 `.sd-Menu-container-*` 行（文本匹配，含 `getMokaPartValue` 自动分辨年月日）→ 点击 → 收起 → 校验 |

`fillOne` 中 Moka 专用分支（第 1 步，content.js:1550）优先于通用分支——Moka 已充分验证，行为不变；其他网站走通用分支。

---

## 8. 代码位置索引（content.js）

| 关注点 | 行号 |
|---|---|
| `fillOne` 分流（Moka → atsx 日期 → 通用 → 普通写值） | 1549–1575 |
| `setValueWithEvents` 统一写值入口 | 1612 |
| `setValueRealistic` 真实输入模拟 | 2903 |
| Moka 专用适配区 | 2409–2640 |
| `isAtsxControl` atsx 隔离 | 2654 |
| `isClickActivator` / `findClickActivator` 激活容器检测 | 2664 / 2708 |
| `isPanelLike` / `collectVisiblePanels` / `findNewPanel` 面板检测 | 2733 / 2744 / 2755 |
| `pickPanelOption` / `clickGenericOption` / `dismissGenericPanel` / `verifyWritten` 面板选值 | 2777 / 2810 / 2824 / 2842 |
| `fillGeneric` 通用填充主流程 | 2854 |
| `isCalendarPanel` / `readCalendarHeader` / `switchCalendarToYearMonth` / `findCalendarDayCell` / `fillCalendarLike` 日历面板选值 | 2907 / 2920 / 2942 / 2962 / 2990 |
| `dispatchInvalidEvent` invalid 确认 | 2420 |
| `getMokaPartValue` 年月日自动分辨 | 2518 |

---

## 9. 注意事项

1. **启发式识别**：`findClickActivator` 是基于特征（onclick/label/role/class）的启发式。若某网站控件的激活容器不符合任何特征（点击无面板、不吃 invalid），会回退普通写值——不会比直接写值更差。
2. **点击副作用**：点击激活容器（label/容器 div）通常只聚焦/激活内部控件，副作用小；兄弟中只认 label，避免误点搜索框/图标。
3. **面板检测是动态的**：通过"点击前后可见面板集合差异"识别新面板，不依赖具体框架的 dropdown class，antd/Element/自研均可覆盖；atsx 面板被显式排除。
4. **invalid 兜底触发**：对走通用路径的控件写值后一律触发 `invalid` + `checkValidity`，无监听者零副作用。
5. **atsx 专用适配优先**：atsx 控件（`atsx-` class）完全由专用逻辑处理，通用逻辑不会触碰（三重隔离，见 §6）。
