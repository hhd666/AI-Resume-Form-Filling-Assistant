以下是整理好的 Markdown 文档，涵盖了 `handleStartFill` 方法中涉及的所有直接调用方法及深层辅助函数。

***

# `handleStartFill` 方法调用链梳理文档

本文档详细梳理了 `content.js` 中核心方法 `handleStartFill()`（约第 108-407 行）在执行过程中调用的所有内部定义函数。包括直接调用的主流程函数以及被间接调用的三级辅助函数。

> **注意**：
> 1. 行号为基于提供代码的估算值，可能存在微小偏差。
> 2. 挂载在 `window` 对象上的外部依赖（如 `diagnostics.*`, `schema.*`）及浏览器原生 API（`chrome.*`）未列入此表。

---

## 1. 主流程直接调用函数

这些函数由 `handleStartFill` 直接调用，构成了自动填表的核心逻辑链路。

### 1.1 准备与扫描阶段

| 方法名 | 约所在行号 | 功能描述 | 输入参数 | 输出/返回值 |
| :--- | :--- | :--- | :--- | :--- |
| **`sendLog`** | ~1853 | 向 Background/Popup 发送日志消息，用于在控制台或 UI 中展示运行状态和诊断信息。 | `level`: string (日志级别)`text`: string (日志文本) | 无 (异步发送) |
| **`requestSelectionRect`** | ~615 | 在页面上显示遮罩层，引导用户通过两次点击框选区域，支持跨滚动页面。 | 无 | `Promise<Object \| null>`返回选区坐标对象 `{left, top, ...}` 或 `null` (取消) |
| **`scanFields`** | ~715 | 扫描页面 DOM，提取表单控件的语义信息（Label, Type, Context），构建运行时对象。 | `options`: Object- `scope`: 'page' \| 'selection'- `selectionRect`: 选区坐标 | `Object``{ fields: Array, runtime: Array }` |
| **`sendStats`** | ~1858 | 更新侧边栏 UI 的进度统计显示。 | `fieldCount`: number`mappedCount`: number`filledCount`: number | 无 |

### 1.2 缓存与 AI 映射阶段

| 方法名 | 约所在行号 | 功能描述 | 输入参数 | 输出/返回值 |
| :--- | :--- | :--- | :--- | :--- |
| **`createMappingCacheSignature`** | ~1653 | 生成页面表单结构的稳定签名，用于判断是否可复用历史缓存。 | `fields`: Array (字段元数据) | `Array` (特征对象数组) |
| **`createMappingCacheKeyFromSignature`**| ~1662 | 结合页面 URL 和字段签名生成唯一的缓存 Key。 | `signature`: Array | `string` (`host:hash`) |
| **`loadMappingCacheEntry`** | ~1785 | 从本地存储读取缓存，并分析命中情况或未命中原因。 | `cacheKey`: string`meta`: Object (诊断信息) | `Promise<Object>``{ entry, hit, reason }` |
| **`buildFieldMappingPayload`** | ~411 | 构建发送给 AI 的 Prompt，精简字段和简历数据以节省 Token。 | `fields`: Array`resumeProfile`: Object | `Object``{ fields: Object, resumeFields: Object }` |
| **`callAI`** | ~1583 | 通过消息通道调用大模型 API，获取原始文本响应。 | `config`: Object`prompt`: string`mode`: string | `Promise<string>` (AI 原始文本) |
| **`parseJsonFromAiText`** | ~1616 | 从 AI 返回的文本中提取有效的 JSON 数据（处理 Markdown 包装等）。 | `text`: string | `Object \| Array` (解析后的 JSON) |
| **`normalizeMappings`** | ~453 | 标准化 AI 返回的映射结果，校验 FieldID 并统一格式。 | `rawMappings`: Array`fields`: Array | `Array` (标准化映射对象数组) |
| **`saveMappingCacheEntry`** | ~1803 | 将新生成的映射结果写入本地存储，并维护缓存池大小。 | `cacheKey`: string`entry`: Object | `Promise<void>` |

### 1.3 数据转换与校验阶段

| 方法名 | 约所在行号 | 功能描述 | 输入参数 | 输出/返回值 |
| :--- | :--- | :--- | :--- | :--- |
| **`isDateLikeField`** | ~1136 | 判断字段是否为日期/时间类型（基于 resumePath 正则匹配）。 | `runtime`: Object | `boolean` |
| **`guardAtsxMapping`** | ~1276 | 纠偏 ATSX 起止时间字段的映射，防止开始/结束时间填反。 | `runtime`: Object`mapping`: Object | `boolean` (正常为 true) |
| **`hasExistingFieldValue`** | ~1096 | 检查字段当前是否已有值，用于增量填充模式。 | `runtime`: Object | `boolean` |
| **`deriveFillValue`** | ~516 | **核心转换逻辑**：根据 transform 规则将简历原始值转换为控件可用值。 | `rawValue`: any`transform`: Object\|string`runtime`: Object | `any` (转换后的值) |
| **`hasMeaningfulFillValue`** | ~566 | 检查转换后的值是否非空且有意义。 | `value`: any | `boolean` |

### 1.4 执行填充阶段

| 方法名 | 约所在行号 | 功能描述 | 输入参数 | 输出/返回值 |
| :--- | :--- | :--- | :--- | :--- |
| **`fillOne`** | ~1146 | 填充单个字段的入口，统一处理成功/失败日志，内部调用 `doFillOne`。 | `runtime`: Object`value`: any | `Promise<Object>``{ filled: boolean, message?: string }` |

---

## 2. 三级调用与深层辅助函数

这些函数不直接被 `handleStartFill` 调用，而是被上述主流程函数（如 `deriveFillValue`, `fillOne` 等）调用，完成具体的数据清洗、格式化或 DOM 操作细节。

### 2.1 数据转换辅助 (被 `deriveFillValue` 调用)

| 方法名 | 约所在行号 | 被谁调用 | 功能描述 | 输入参数 | 输出/返回值 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **`normalizeTransform`** | ~490 | `deriveFillValue` | 将 AI 返回的 transform 规则标准化为统一对象格式。 | `transform`: any | `Object` (标准转换规则) |
| **`getDatePart`** | ~1305 | `deriveFillValue` | 从日期字符串中提取年、月或日。 | `dateStr`: string`part`: 'year'\|'month'\|'day' | `string` (提取的部分) |
| **`getPhonePart`** | ~1328 | `deriveFillValue` | 从电话号码中提取国家码或本机号码。 | `phoneStr`: string`part`: 'countryCode'\|'nationalNumber' | `string` (提取的部分) |
| **`joinValue`** | ~1345 | `deriveFillValue` | 将数组值拼接为字符串，或原样返回字符串。 | `value`: any`separator`: string | `string` |
| **`normalizeCheckboxCandidates`** | ~550 | `deriveFillValue` | 将多选项值标准化为数组格式。 | `value`: any | `Array<string>` |
| **`hasSourceValue`** | ~540 | `deriveFillValue` | 检查原始数据源是否有有效值。 | `value`: any | `boolean` |

### 2.2 特定平台填充辅助 (被 `fillOne` / `doFillOne` 调用)

| 方法名 | 约所在行号 | 被谁调用 | 功能描述 | 输入参数 | 输出/返回值 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **`preparePeriodMonthDesired`** | ~1290 | `fillAtsxPeriodMonth` | 解析并标准化 ATSX 年月控件的目标值。 | `value`: any | `string` (`YYYY-MM`) |
| **`fillAtsxPeriodMonth`** | ~1370 | `doFillOne` | 专门处理 ATSX 系统的年月选择控件（点击面板选择）。 | `runtime`: Object`desired`: string | `Promise<boolean>` |
| **`fillReadonlyDateRuntime`** | ~1160 | `doFillOne` | 处理只读日期控件，通过模拟点击日历面板进行填充。 | `runtime`: Object`desired`: string | `Promise<boolean>` |
| **`fillMokaSelect`** | ~2830 | `doFillOne` | 专门处理 Moka 系统的下拉选择控件。 | `runtime`: Object`desired`: string | `Promise<boolean>` |
| **`fillPhSelect`** | ~2750 | `doFillOne` | 专门处理 Phoenix 系统的日期选择控件。 | `runtime`: Object`desired`: string | `Promise<boolean>` |
| **`fillGeneric`** | ~3150 | `doFillOne` | 通用控件填充策略，尝试寻找激活器并点击面板选项。 | `runtime`: Object`desired`: string | `Promise<boolean>` |
| **`setValueWithEvents`** | ~1450 | `doFillOne` | 通用文本输入框填充，触发 input/change/invalid 事件。 | `el`: HTMLElement`value`: string`runtime`: Object | `Promise<boolean>` |

### 2.3 DOM 操作与事件模拟辅助 (被各类 Fill 函数调用)

| 方法名 | 约所在行号 | 被谁调用 | 功能描述 | 输入参数 | 输出/返回值 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **`clickLikeUser`** | ~1550 | 多种 Fill 函数 | 模拟真实用户点击行为（mousedown, mouseup, click）。 | `el`: HTMLElement | 无 |
| **`scrollIntoView`** | ~1430 | 多种 Fill 函数 | 将元素滚动到可视区域中心。 | `el`: HTMLElement | 无 |
| **`dispatchInvalidEvent`** | ~1600 | `setValueWithEvents` | 触发自定义 invalid 事件，让某些框架认可脚本写入的值。 | `el`: HTMLElement | 无 |
| **`dispatchInputEvents`** | ~1520 | `setValueRealistic` | 触发完整的输入事件链（composition, input, change）。 | `el`: HTMLElement | 无 |
| **`setNativeValue`** | ~1500 | `setValueRealistic` | 绕过 React/Vue 代理，直接设置 DOM 元素的 value 属性。 | `element`: HTMLElement`value`: string | 无 |
| **`sleep`** | ~1575 | 多种异步函数 | 简单的延时等待，用于等待 DOM 渲染或动画完成。 | `ms`: number | `Promise<void>` |

### 2.4 缓存与工具辅助

| 方法名 | 约所在行号 | 被谁调用 | 功能描述 | 输入参数 | 输出/返回值 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **`normalizeCacheText`** | ~1680 | `createStableCacheFieldSignature` | 清洗字段文本，去除无关字符，生成稳定的缓存特征。 | `value`: string | `string` |
| **`hashString`** | ~1740 | `createMappingCacheKeyFromSignature` | 对字符串进行哈希计算，生成短键。 | `text`: string | `string` (Hex Hash) |
| **`describeMappingCacheLookup`** | ~1755 | `loadMappingCacheEntry` | 生成缓存查找结果的诊断描述信息。 | `cache`: Object`cacheKey`: string`meta`: Object | `Object` (含 reason) |