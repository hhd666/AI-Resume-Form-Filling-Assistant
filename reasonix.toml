[permissions]
allow = ["Bash=cd \"D:\\ProgramGreen\\js\\AI-Resume-Form-Filling-Assistant-master\" && node --test tests/ 2>&1 | tail -15", "Bash=cd \"D:\\ProgramGreen\\js\\AI-Resume-Form-Filling-Assistant-master\" && node --test tests/ 2>&1 | head -40", "Bash=cd \"D:\\ProgramGreen\\js\\AI-Resume-Form-Filling-Assistant-master\" && cat > /tmp/moka-check.js <<'EOF'\nconst fs = require(\"node:fs\");\nconst source = fs.readFileSync(\"content.js\", \"utf8\");\n\nfunction extract(name) {\n  const sig = `function ${name}(`;\n  const start = source.indexOf(sig);\n  if (start === -1) throw new Error(`not found: ${name}`);\n  // 找到匹配的右括号结束（简化：按函数体花括号配对）\n  let depth = 0, i = source.indexOf(\"{\", start), end = -1;\n  for (; i < source.length; i++) {\n    if (source[i] === \"{\") depth++;\n    else if (source[i] === \"}\") { depth--; if (depth === 0) { end = i + 1; break; } }\n  }\n  return source.slice(start, end);\n}\n\nconst code = `\n${extract(\"clickLikeUser\")}\n${extract(\"findMokaSdInputLabel\")}\n${extract(\"isMokaSdInput\")}\n${extract(\"dispatchInvalidEvent\")}\n`;\n\n// ---- 模拟 DOM ----\nfunction makeEl(className, parent = null) {\n  return { className, parent, closest(sel) {\n    let node = this;\n    while (node) {\n      if (sel.includes(\"class*=\")) {\n        const cls = sel.match(/class\\*=\"([^\"]+)\"/)[1];\n        if (String(node.className || \"\").includes(cls)) return node;\n      }\n      node = node.parent;\n    }\n    return null;\n  }};\n}\n\nconst label = makeEl(\"sd-Input-container-2zU-8\");\nconst input = makeEl(\"sd-Input-input-QsLkW sd-Input-common-input-21SDH\", label);\nconst plainInput = makeEl(\"normal-input\");\nconst remoteInput = makeEl(\"sd-Input-input-x\", null); // 无 label 包裹\n\nlet invalidFired = false, validityChecked = false;\nconst tracked = {\n  className: \"sd-Input-input-Q\", parent: makeEl(\"sd-Input-container-abc\"),\n  dispatchEvent(e) { if (e.type === \"invalid\") invalidFired = true; return true; },\n  checkValidity() { validityChecked = true; return false; },\n};\n\nconst ctx = { MouseEvent, Event, window: {}, document: {} };\nconst fn = new Function(\"MouseEvent\", \"Event\", code + `\n  return {\n    findMokaSdInputLabel, isMokaSdInput, dispatchInvalidEvent,\n    r1: Boolean(findMokaSdInputLabel(input)),\n    r2: isMokaSdInput(input),\n    r3: isMokaSdInput(plainInput),\n    r4: isMokaSdInput(remoteInput),\n  };\n`);\nconst out = fn(MouseEvent, Event);\nconsole.log(\"label 包裹的 input 找到 label:\", out.r1, \"(期望 true)\");\nconsole.log(\"label 包裹的 input 判定 Moka:\", out.r2, \"(期望 true)\");\nconsole.log(\"普通 input 判定 Moka:\", out.r3, \"(期望 false)\");\nconsole.log(\"无 label 的 sd-Input-input-* 判定 Moka:\", out.r4, \"(期望 true)\");\nout.dispatchInvalidEvent(tracked);\nconsole.log(\"invalid 事件触发:\", invalidFired, \"(期望 true)\");\nconsole.log(\"checkValidity 调用:\", validityChecked, \"(期望 true)\");\nEOF\nnode /tmp/moka-check.js", "Bash=cd \"D:\\ProgramGreen\\js\\AI-Resume-Form-Filling-Assistant-master\" && cat > /tmp/moka-check.js <<'EOF'\nconst fs = require(\"node:fs\");\nconst source = fs.readFileSync(\"content.js\", \"utf8\");\n\nfunction extract(name) {\n  const sig = `function ${name}(`;\n  const start = source.indexOf(sig);\n  if (start === -1) throw new Error(`not found: ${name}`);\n  let depth = 0, i = source.indexOf(\"{\", start), end = -1;\n  for (; i < source.length; i++) {\n    if (source[i] === \"{\") depth++;\n    else if (source[i] === \"}\") { depth--; if (depth === 0) { end = i + 1; break; } }\n  }\n  return source.slice(start, end);\n}\n\nconst code = `\n${extract(\"clickLikeUser\")}\n${extract(\"findMokaSdInputLabel\")}\n${extract(\"isMokaSdInput\")}\n${extract(\"dispatchInvalidEvent\")}\n\nfunction makeEl(className, parent = null) {\n  return { className, parent, closest(sel) {\n    let node = this;\n    while (node) {\n      const cls = sel.match(/class\\\\*=\"([^\"]+)\"/);\n      if (cls && String(node.className || \"\").includes(cls[1])) return node;\n      node = node.parent;\n    }\n    return null;\n  }};\n}\n\nconst label = makeEl(\"sd-Input-container-2zU-8\");\nconst input = makeEl(\"sd-Input-input-QsLkW sd-Input-common-input-21SDH\", label);\nconst plainInput = makeEl(\"normal-input\");\nconst remoteInput = makeEl(\"sd-Input-input-x\");\n\nlet invalidFired = false, validityChecked = false;\nconst tracked = {\n  className: \"sd-Input-input-Q\",\n  parent: makeEl(\"sd-Input-container-abc\"),\n  dispatchEvent(e) { if (e.type === \"invalid\") invalidFired = true; return true; },\n  checkValidity() { validityChecked = true; return false; },\n};\n\nreturn {\n  r1: Boolean(findMokaSdInputLabel(input)),\n  r2: isMokaSdInput(input),\n  r3: isMokaSdInput(plainInput),\n  r4: isMokaSdInput(remoteInput),\n};\n`;\n\nglobalThis.MouseEvent = class { constructor(t){ this.type = t; } };\nconst out = new Function(code)();\nconsole.log(\"label 包裹的 input 找到 label:\", out.r1, \"(期望 true)\");\nconsole.log(\"label 包裹的 input 判定 Moka:\", out.r2, \"(期望 true)\");\nconsole.log(\"普通 input 判定 Moka:\", out.r3, \"(期望 false)\");\nconsole.log(\"无 label 的 sd-Input-input-* 判定 Moka:\", out.r4, \"(期望 true)\");\n\n// dispatchInvalidEvent 行为验证（单独提取，避免全局副作用）\nconst src2 = fs.readFileSync(\"content.js\", \"utf8\");\nconst s = src2.indexOf(\"function dispatchInvalidEvent(\");\nconst e = s + src2.slice(s).indexOf(\"function parseDateParts\");\nconst fnCode = src2.slice(s, e);\nconst evt = new (class { constructor(t){ this.type = t; } })(\"invalid\");\nconst mockEl = {\n  dispatchEvent: (x) => { if (x.type === \"invalid\") console.log(\"invalid 事件已触发（期望出现）\"); return true; },\n  checkValidity: () => { console.log(\"checkValidity 已调用（期望出现）\"); return false; },\n};\nnew Function(\"Event\", fnCode + `\\ndispatchInvalidEvent(mockEl);`)(class { constructor(t){ this.type = t; } });\nEOF\nnode /tmp/moka-check.js", "Bash=cd \"D:\\ProgramGreen\\js\\AI-Resume-Form-Filling-Assistant-master\" && cat > /tmp/moka-check.js <<'EOF'\nconst fs = require(\"node:fs\");\nconst source = fs.readFileSync(\"content.js\", \"utf8\");\n\nfunction extract(name) {\n  const sig = `function ${name}(`;\n  const start = source.indexOf(sig);\n  if (start === -1) throw new Error(`not found: ${name}`);\n  let depth = 0, i = source.indexOf(\"{\", start), end = -1;\n  for (; i < source.length; i++) {\n    if (source[i] === \"{\") depth++;\n    else if (source[i] === \"}\") { depth--; if (depth === 0) { end = i + 1; break; } }\n  }\n  return source.slice(start, end);\n}\n\nconst code = `\n${extract(\"clickLikeUser\")}\n${extract(\"findMokaSdInputLabel\")}\n${extract(\"isMokaSdInput\")}\n\nfunction makeEl(className, parent = null) {\n  return { className, parent, closest(sel) {\n    let node = this;\n    while (node) {\n      const m = sel.match(/class\\*=\"([^\"]+)\"/);\n      if (m && String(node.className || \"\").includes(m[1])) return node;\n      node = node.parent;\n    }\n    return null;\n  }};\n}\n\nconst label = makeEl(\"sd-Input-container-2zU-8\");\nconst input = makeEl(\"sd-Input-input-QsLkW sd-Input-common-input-21SDH\", label);\nconst plainInput = makeEl(\"normal-input\");\nconst remoteInput = makeEl(\"sd-Input-input-x\");\n\nreturn {\n  r1: Boolean(findMokaSdInputLabel(input)),\n  r2: isMokaSdInput(input),\n  r3: isMokaSdInput(plainInput),\n  r4: isMokaSdInput(remoteInput),\n};\n`;\n\nglobalThis.MouseEvent = class { constructor(t){ this.type = t; } };\nconst out = new Function(code)();\nconsole.log(\"1) label 包裹的 input 找到 label:\", out.r1, \"(期望 true)\");\nconsole.log(\"2) label 包裹的 input 判定 Moka:\", out.r2, \"(期望 true)\");\nconsole.log(\"3) 普通 input 判定 Moka:\", out.r3, \"(期望 false)\");\nconsole.log(\"4) 无 label 的 sd-Input-input-* 判定 Moka:\", out.r4, \"(期望 true)\");\n\n// dispatchInvalidEvent 行为：触发 invalid + 调用 checkValidity\nconst s = source.indexOf(\"function dispatchInvalidEvent(\");\nconst e = s + source.slice(s).indexOf(\"function parseDateParts\");\nconst fnCode = source.slice(s, e) + `\nconst mockEl = {\n  dispatchEvent: (x) => { if (x.type === \"invalid\") console.log(\"5) invalid 事件已触发 (期望出现)\"); return true; },\n  checkValidity: () => { console.log(\"6) checkValidity 已调用 (期望出现)\"); return false; },\n};\ndispatchInvalidEvent(mockEl);\n`;\nnew Function(\"Event\", fnCode)(class { constructor(t){ this.type = t; } });\nEOF\nnode /tmp/moka-check.js", "Bash=cd \"D:\\ProgramGreen\\js\\AI-Resume-Form-Filling-Assistant-master\" && node -e \"\nconst fs = require('fs');\nconst source = fs.readFileSync('content.js','utf8');\nconst s = source.indexOf('function findMokaSdInputLabel(');\nconsole.log('--- findMokaSdInputLabel 定义 ---');\nconsole.log(source.slice(s, s + 400));\n\"", "Bash=grep -n \"class\" /tmp/moka-check.js | head; echo \"---\"; node -e \"\nconst re = /class\\*=\\\"([^\\\"]+)\\\"/;\nconst m = re.exec('label[class*=\\\"sd-Input-container-\\\"]');\nconsole.log('正则匹配结果:', m && m[1]);\n\"", "Bash=cd \"D:\\ProgramGreen\\js\\AI-Resume-Form-Filling-Assistant-master\" && node -e \"\nconst fs = require('fs');\nconst source = fs.readFileSync('content.js','utf8');\nfunction extract(name) {\n  const sig = 'function ' + name + '(';\n  const start = source.indexOf(sig);\n  let depth = 0, i = source.indexOf('{', start), end = -1;\n  for (; i < source.length; i++) {\n    if (source[i] === '{') depth++;\n    else if (source[i] === '}') { depth--; if (depth === 0) { end = i + 1; break; } }\n  }\n  return source.slice(start, end);\n}\nconst fnCode = extract('findMokaSdInputLabel');\nconsole.log('提取的代码:');\nconsole.log(fnCode);\nconst label = { className: 'sd-Input-container-2zU-8', parent: null,\n  closest(sel){ const m = sel.match(/class\\*=\\\"([^\\\"]+)\\\"/);\n    if (m && String(this.className||'').includes(m[1])) return this; return this.parent && this.parent.closest ? this.parent.closest(sel) : null; } };\nconst input = { className: 'sd-Input-input-Q', parent: label,\n  closest(sel){ const m = sel.match(/class\\*=\\\"([^\\\"]+)\\\"/);\n    if (m && String(this.className||'').includes(m[1])) return this; return this.parent && this.parent.closest ? this.parent.closest(sel) : null; } };\nconsole.log('closest 调用结果:', fnCode + '; findMokaSdInputLabel(input)', new Function(fnCode + '; return findMokaSdInputLabel(input);')() ? '找到' : '未找到');\n\"", "Bash=cd \"D:\\ProgramGreen\\js\\AI-Resume-Form-Filling-Assistant-master\" && node /tmp/moka-check.js 2>&1 | head -6; echo \"==== 用真实浏览器 CSS 选择器语义验证 closest ====\"; node -e \"\nconst fs = require('fs');\nconst source = fs.readFileSync('content.js','utf8');\nconst s = source.indexOf('function findMokaSdInputLabel(');\nconst fnCode = source.slice(s, source.indexOf('function isMokaSdInput('));\n\n// 模拟真实 DOM closest 语义（含标签名 + 属性子串匹配）\nfunction makeEl(tag, className, parent = null) {\n  return { tagName: tag.toUpperCase(), className, parent,\n    closest(sel) {\n      let node = this;\n      while (node) {\n        const tagM = sel.match(/^([a-z]+)\\[/);\n        const clsM = sel.match(/class\\*=\\\"([^\\\"]+)\\\"/);\n        const tagOk = !tagM || node.tagName === tagM[1].toUpperCase();\n        const clsOk = !clsM || String(node.className||'').includes(clsM[1]);\n        if (tagOk && clsOk) return node;\n        node = node.parent;\n      }\n      return null;\n    } };\n}\nconst label = makeEl('label', 'sd-Input-container-2zU-8');\nconst input = makeEl('input', 'sd-Input-input-QsLkW sd-Input-common-input-21SDH', label);\nconst r = new Function(fnCode + '; return findMokaSdInputLabel(input);')();\nconsole.log('真实语义下找到 label:', Boolean(r), '| 返回节点 class:', r && r.className, '(期望 sd-Input-container-2zU-8)');\n\"", "Bash=cd \"D:\\ProgramGreen\\js\\AI-Resume-Form-Filling-Assistant-master\" && node -e \"\nconst fs = require('fs');\nconst s = fs.readFileSync('content.js','utf8');\nconst i = s.indexOf('fillMokaSelect');\nconsole.log('首次出现位置:', i);\nconsole.log(JSON.stringify(s.slice(i-40, i+30)));\n\"", "Bash=cd \"D:\\ProgramGreen\\js\\AI-Resume-Form-Filling-Assistant-master\" && node -e \"\nconst fs = require('fs');\nconst source = fs.readFileSync('content.js','utf8');\nconst i = source.indexOf('async function fillMokaSelect(');\nconsole.log('async 定义位置:', i);\nconsole.log(source.slice(i, i + 80));\n\" && grep -n \"async function fillMokaSelect\" content.js"]
