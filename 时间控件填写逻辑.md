# 时间控件填写逻辑总览

本文档梳理 content.js 对**所有时间/日期类控件**的填写逻辑：已适配平台（atsx、Moka、通用日期面板）与通用平台（激活点击 + 日历/下拉面板），包括各自的分流条件、完整流程、时间线与失败处理，以及针对"失败时卡顿"的优化说明。

---

## 1. 总览：时间控件分流（`fillOne`，content.js:1467 起）

```
fillOne(runtime, value)
 ├─ runtime.kind === "atsx_period_month"  → fillAtsxPeriodMonth   （atsx 起止年月，§2）
 ├─ isMokaSelectLike(runtime.el)          → fillMokaSelect        （Moka 年月下拉，§4）
 ├─ isReadonlyDateLikeRuntime(runtime)    → fillReadonlyDateRuntime（通用只读日期面板，§3）
 ├─ findClickActivator(runtime.el)        → fillGeneric           （通用激活：下拉/选项面板，§5；**日期字段跳过**）
 └─ 兜底                                  → setValueWithEvents    （普通写值，§6）
```

优先级：**专用（atsx）> 专用（Moka）> 通用只读日期 > 通用激活（仅非日期字段）> 普通写值**。

**填充顺序**（`handleStartFill`，content.js:250 起）：非日期字段先填、日期/时间控件**最后处理**（日期控件失败率高且耗时，放最后避免阻塞其他字段）。判定：`isDateLikeField(runtime)`（content.js:1447）——**按映射路径**：`runtime.resumePath` 以 `.date` 结尾（`startDate`/`endDate`/`date`/`graduationDate` 等）即日期字段；`resumePath` 由 `handleStartFill` 在填充前从 mapping 附加到 runtime（content.js:222 起）。

**日期字段的填写路径**（当前策略）：只保留**已适配平台**（atsx `fillAtsxPeriodMonth`、Moka `fillMokaSelect`、通用只读日期 `fillReadonlyDateRuntime`）和**普通写值**（`setValueWithEvents`）——**不走通用激活 `fillGeneric`**（日历/下拉面板路径对日期暂关闭）。

各路径的**失败均立即 `return false`**（不抛错、不阻塞后续字段），由 `fillOne` 返回 `{ filled: false }` 并继续填下一个字段——"卡一下"的观感来自各路径内部的**等待/轮询超时**（§7），而非流程阻塞。

---

## 2. atsx 起止年月（`fillAtsxPeriodMonth`，content.js:1853）

**触发**：`runtime.kind === "atsx_period_month"`（智联招聘 ATS 系专用，见 `atsx-date-picker-support.md`）。

**流程与时间线**：

| 步骤 | 操作 | 等待（最坏） | 失败点 |
|---|---|---|---|
| 1 | 解析 `YYYY-MM` → year/month | — | 非日期格式 |
| 2 | 定位 begin/end 标签 | — | 标签未找到 |
| 3 | 关闭残留面板 | 80ms 兜底 | — |
| 4 | 点击标签打开面板 | `sleep(150)` | — |
| 5 | 重试定位面板（data-cy 精确匹配 + 排除 hidden） | 5 × 120ms = 600ms | 面板未注入/带 hidden |
| 6 | 点年份（`clickAtsxPeriodItem`，内容分类 + data-cy） | 内部 `sleep(100)` | 年份项未找到 |
| 7 | 点年份后重查面板 | 5 × 100ms = 500ms | — |
| 8 | `waitForAtsxYearSelected`（确认性，未确认也继续） | 5 × 100ms = 500ms | 仅日志提示 |
| 9 | 点月份（最多 **2** 次尝试） | 每次：点击 `sleep(100)` + `waitForAtsxLabel` 6 × 100ms = 600ms + 重试时重查面板/`sleep(150)` | 月份项未找到 / 标签 600ms 未更新 |

**最坏总时长 ~3.2s，失败时主要耗时在步骤 9（最多 2 次 × ~850ms）与步骤 5/7（600+500ms）**。

**关键校验**：`waitForAtsxLabel` 轮询标签 `span[data-cy=year/month]` 变为目标值；点月份后**不主动发 Escape**（组件自行销毁面板，Escape 会被当作取消回滚）。

---

## 3. 通用只读日期面板（`fillReadonlyDateRuntime`，content.js:1675）

**触发**：`fillRuntime.isReadonlyDateLikeRuntime(runtime)`——input 只读且标签含日期关键字（入学/毕业/开始/结束/日期等）。适用于非 atsx、非 Moka 的只读日期控件（如 mtd/antd 旧版等）。

**流程与时间线**：

| 步骤 | 操作 | 等待 | 失败点 |
|---|---|---|---|
| 1 | `setValueWithEvents` 直接写值 | 内部 ~100ms | 写值不被认可（只读） |
| 2 | 点击触发元素（`.mtd-input-affix-wrapper` 或 input 自身） | `sleep(120)` | — |
| 3 | 失败再点一次 input 自身 | `sleep(120)` | — |
| 4 | `findVisibleDatePanel`（class 含 picker/calendar/dialog + 文本含 `\d{4}年`/`N月`） | — | 面板未找到（纯数字面板不匹配） |
| 5 | `movePickerToYear` 切年份（按方向点 prev/next 按钮） | 最多 24 × 90ms ≈ 2.2s | 年份切换控件未找到 |
| 6 | 点月份（`clickPanelCell` 文本匹配 `N月`） | `sleep(90)` | 月份格未找到 |
| 7 | 点日期（有 day 时） | `sleep(90)` | 日期格未找到 |
| 8 | `matchesWrittenValue` 校验 | — | 值不匹配 |

**最坏 ~3s**（跨年远时年份切换为主）。

**局限**：面板文本必须含"年/月"字样（`\d{4}年|N月` 正则）——纯数字面板（如 atsx）不适用（atsx 已走专用逻辑）；antd 日历（`ant-calendar-date-panel` 含 "2024年"/"9月"）可命中 §5 的日历路径而非本路径。

---

## 4. Moka 年月下拉（`fillMokaSelect`，content.js:2566）

**触发**：`isMokaSelectLike(runtime.el)`——Moka 网申平台的 `sd-Select` 或带 `sd-Input-has-addon-` 的下拉型 input。

**流程与时间线**：

| 步骤 | 操作 | 等待 | 失败点 |
|---|---|---|---|
| 1 | `getMokaPartValue` 自动分辨年/月/日提取目标 | — | — |
| 2 | 收起残留下拉 | — | — |
| 3 | 点击 label 展开下拉 | `sleep(60)` + `sleep(160)` | — |
| 4 | 面板未出现则重试点击 | `sleep(160)` | 面板未打开 |
| 5 | 面板内找 `.sd-Menu-container-*` 行（文本匹配） | — | 目标选项未找到 |
| 6 | 点击选项 | `sleep(120)` | — |
| 7 | 收起 + 校验 `el.value` | `sleep(80)` | 值未更新 |

**最坏 ~1.3s**。

---

## 5. 通用激活（`fillGeneric`，content.js:2854）——含日历面板

**触发**：`!isDateLikeField(runtime) && !isAtsxControl(el) && findClickActivator(el)`——任意网站**非日期**字段，input 自身/父链/label 兄弟存在 click 激活容器。**日期字段不进入本路径**（见 §1 日期字段策略）。

**流程与时间线**：

| 步骤 | 操作 | 等待 | 失败点 |
|---|---|---|---|
| 1 | `findClickActivator` 找激活容器 | — | 无激活容器（落兜底写值） |
| 2 | 记录点击前面板集合 | — | — |
| 3 | `clickLikeUser` 点击激活容器 | `sleep(160)` | — |
| 4 | `findNewPanel` 对比集合差异 | — | 无新面板 |
| 5a | **日历面板**（`isCalendarPanel`）→ `fillCalendarLike`：切年月 → 点日期 | 切换最多 24 × 90ms ≈ 2.2s + 点击 `sleep(90)` | 年月切换失败 / 日期格未找到 |
| 5b | **普通选项面板** → `clickGenericOption` 文本匹配选值 | `sleep(100)` | 选项未找到 |
| 6 | 收起 + 校验 | `sleep(80)` | 值未更新 |
| 7 | 无面板 → `setValueRealistic` 写值 + `dispatchInvalidEvent` | `sleep(80)` | 写值不被认可 |

**最坏 ~2.8s**（日历跨年远时）。

**日历面板细节**（antd 等，content.js:2907 起）：`readCalendarHeader` 读当前年月 → `switchCalendarToYearMonth` 用 prev/next 月按钮切换 → `findCalendarDayCell` 按 `td[title="2024年9月1日"]` 或同月文本找日期格 → 点击。

---

## 6. 普通写值兜底（`setValueWithEvents`，content.js:1617）

**触发**：以上路径均不适用或失败后的兜底。

**流程**：`scrollIntoView` →（Moka 非 select 的 sd-Input 先点 label 激活，`sleep(30)`）→ `focus` + 临时解除 readonly → `setValueRealistic`（原生 setter + `execCommand("insertText")` 或事件链）→（Moka 触发 `dispatchInvalidEvent`）→ `blur` → `sleep(60)` → `matchesWrittenValue` 校验。

**最坏 ~200ms**。对框架受控组件通常只改到 `value` 属性而不被认可（校验失败），因此时间控件应尽量命中前四层专用/通用路径。

---

## 7. "卡一下"的来源与本次优化

### 失败时卡顿的来源（旧值）

| 位置 | 旧最坏等待 | 问题 |
|---|---|---|
| atsx 月份重试（3 次） | 3 × (点击 100ms + 轮询 1s + 重查 500ms) ≈ 5s | 轮询等满 1s × 3 次 |
| atsx `waitForAtsxYearSelected` | 8 × 100ms = 800ms | 确认性检查等满 |
| 通用日期 `movePickerToYear` | 24 × 120ms ≈ 2.9s | 跨年远时逐月点 |
| 通用日历 `switchCalendarToYearMonth` | 24 × 120ms ≈ 2.9s | 跨年远时逐月点 |
| 通用日期/日历点击后 | 3 处 × 120ms | 固定等待偏长 |

**本次优化（content.js）**：
1. atsx 月份重试 **3 → 2 次**，标签轮询 **10×100ms → 6×100ms**（失败省 ~2.5s）；
2. atsx `waitForAtsxYearSelected` **8 → 5 次**（省 300ms，确认性检查）；
3. 通用日期 `movePickerToYear` / 通用日历 `switchCalendarToYearMonth` / 点月/日期后等待 **120ms → 90ms**。

优化后 atsx 失败最坏约 3.2s（原 ~6.5s），通用日历跨年约 2.2s（原 2.9s）。

### 失败率高的排查方向（按命中率从高到低）

1. **atsx 面板定位失败**（`[atsx] 打开面板失败`）：data-cy 严格匹配 + 排除 `-hidden` + `isVisible`。若组件渲染 >600ms 或结构改版（data-cy 后缀变化、hidden class 改名）会失败。看 `[atsx] findAtsxPeriodPanel(cy=...) 候选面板数 = 0`。
2. **atsx 月份点击后标签未更新**（`[atsx] 月份填写第 N/2 次尝试` 两次都失败）：组件更新慢或点击落空。看 `[atsx] 第 N 次尝试后标签值 = ...（目标 ...）`。
3. **通用只读日期面板未找到**（`[日期] 打开面板失败`）：面板文本不含"年/月"字样（纯数字面板）。
4. **通用日历切换失败**：`switchCalendarToYearMonth` 找不到 prev/next 按钮（非 antd 结构变体）。
5. **校验严格**：标签/`el.value` 与目标不完全一致（如组件写回格式不同）→ 实际已填但判定失败。

诊断统一看 Console 的 `[atsx]` / `[日期]` 前缀日志（均为字符串快照，元素销毁后仍可读）。

---

## 8. 相关文档与代码索引

- atsx 专用：`atsx-date-picker-support.md`（完整结构/流程/行号）
- 通用激活 + 日历/下拉面板：`通用控件.md`（§4.4 面板选值、§4.5 日历面板选值）
- 本总览对应代码：
  - `fillOne` 分流：content.js:1467
  - `fillAtsxPeriodMonth`：1853
  - `fillReadonlyDateRuntime`：1675
  - `fillMokaSelect`：2566
  - `fillGeneric`：2854
  - `setValueWithEvents`：1617
